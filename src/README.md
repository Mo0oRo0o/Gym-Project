download this libraries :-

1- django 6.0.4

   download by this command via terminal => python -m pip install django==6.0.4

2 - django-debug-toolbar
   
   download by this command via terminal => python -m pip install django-debug-toolbar

